import argparse
from src.model import ImageClassifier, CheckpointManager
from src.utils import get_device

def main():
    # Parse arguments, load data, and initialize model

    # Train the model

    # Save checkpoint using CheckpointManager

if __name__ == '__main__':
    main()
